const Pill = ({ children }) => (
  <span className="inline-block px-3 py-1 text-xs font-medium text-gray-300 bg-gray-800 rounded-full border border-gray-700">
    {children}
  </span>
);

export default Pill;
